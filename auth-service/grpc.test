docker run --rm -it \
  --network host \
  -v "$PWD/auth-service/proto:/proto" \
  fullstorydev/grpcurl \
  -plaintext \
  -import-path /proto \
  -proto auth.proto \
  -d '{"token":"<jwt_token>"}' \
  localhost:50050 auth.AuthService/ValidateToken
